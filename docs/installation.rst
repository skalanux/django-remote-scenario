============
Installation
============

At the command line::

    $ easy_install django-remote-scenario

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-remote-scenario
    $ pip install django-remote-scenario